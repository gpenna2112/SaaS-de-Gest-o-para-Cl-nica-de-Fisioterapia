# jobs

Definições de jobs pg-boss (ADR-0008): envio das confirmações de sessão via WhatsApp e seus retries, lendo da tabela de outbox (`notifications`).

**Status:** deliberadamente vazio nesta etapa. pg-boss depende de uma conexão real de banco e de suas próprias tabelas — entra junto com `src/db`, não antes.
